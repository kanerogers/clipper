[package]
edition = "2021"
name = "common"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bitflags = "2.3.3"
glam = "0.22.0"
rand = "0.8"

[lib]
crate-type = ["dylib"]
